# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Gradle
/android/.gradle/
/android/build/

# 本地调试产物（截图 / 日志 / 录音 / UI dump）
/logs/
/*.png
/*.txt
/*.pcm
/ui*.xml
/_brand_backup_*/

# 编译期注入的密钥文件（--dart-define-from-file 用）。
# ⚠️ 真实密钥只允许放在这类被忽略的文件里，或直接写在命令行上。
/keys.json
*.keys.json

# 说明：lib/config/iflytek_config.dart 是**受版本控制的空模板**，
# 它只从 --dart-define 取值，本身不含密钥，所以这里不再忽略它。
# 详见该文件顶部注释与 README「语音识别」一节。

